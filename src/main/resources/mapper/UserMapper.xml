<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wordmemory.platform.mapper.UserMapper">

    <insert id="insertUser" parameterType="com.wordmemory.platform.entity.User"
            useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (username, password_hash, salt)
        VALUES (#{username}, #{passwordHash}, #{salt})
    </insert>

    <select id="findByUsername" resultType="com.wordmemory.platform.entity.User">
        SELECT user_id, username, password_hash, salt, score, total_likes, created_at
        FROM users
        WHERE username = #{username}
    </select>

    <select id="findById" resultType="com.wordmemory.platform.entity.User">
        SELECT user_id, username, password_hash, salt, score, total_likes, created_at
        FROM users
        WHERE user_id = #{userId}
    </select>

    <select id="listRanking" resultType="com.wordmemory.platform.entity.User">
        SELECT user_id, username, score, total_likes, created_at
        FROM users
        ORDER BY score DESC, total_likes DESC, created_at ASC
    </select>

    <update id="incrTotalLikes">
        UPDATE users
        SET total_likes = total_likes + 1
        WHERE user_id = #{userId}
    </update>

    <update id="addScore">
        UPDATE users
        SET score = score + #{delta}
        WHERE user_id = #{userId}
    </update>

</mapper>
